# MonzoExpenses

Add the Step Functions plugin for the Serverless Framework:

serverless plugin install --name serverless-step-functions

Add support for AWS Pseudo Parameters:

npm install --save-dev serverless-pseudo-parameters

test commit
